import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFFF7F2E8); // very light warm cream
  static const Color cardBackground = Color(0xFFFCFBF8); // almost white for cards

  // Primary accents
  static const Color primary = Color(0xFF8DB7A3); // main mint
  static const Color primaryMuted = Color(0xFFBFD6C9); // lighter mint

  // Text
  static const Color textPrimary = Color(0xFF1F3B32); // dark desaturated green/grey
  static const Color textSecondary = Color(0xFF6B7C74); // muted text

  // Navigation / bars
  static const Color navBarBackground = Color(0xFFF0ECE4); // slightly different cream
  static const Color navBarActive = primary;
  static const Color navBarInactive = Color(0xFF9AAEA6);

  // Borders / subtle separators
  static const Color borderSubtle = Color(0xFFE7E3DB);

  // Other accents
  static const Color warmAccent = Color(0xFFF5B94C); // pyramid warm yellow
  // Accent colors used for action buttons
  static const Color accentGreen = Color(0xFF00C48C);
  static const Color accentRed = Color(0xFFFF4C5B);
}
